// ABOUTME: Template for Supabase credentials. Copy to config.js and fill in.
// ABOUTME: config.js is gitignored; never commit real credentials.

window.SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR_PUBLIC_ANON_KEY';

// Admin code gates results entry. Keep this private — anyone with it can post results.
window.ADMIN_CODE = 'change-me';
